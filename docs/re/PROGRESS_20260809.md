# CMP 170HX / 32G wall — progress report (2026-08-09)

## Goal

Load **patch A** (NOP `dmaUpdateVASpace` @ tu10x fwimage+`0x1b54664`) into **running GSP-RM** to fix multi-segment PTE bug past phantom hole `[36G,44G)`.

## Blocked paths (do not retry without new evidence)

| Path | Result |
|------|--------|
| Disk patch `gsp_tu10x.bin` | Booter `mbox=0xb` |
| Pre-radix RAM patch + forgive mbox | Host `0x0`, GSP hang (`GspMsgQueue`) — verify abort, patched image not in WPR |
| `RMCmpBooterSkipApp=1` | Booter `0x0`, no GSP-RM load → hang |
| BAR0 PRAMIN @ `gspFwOffset` | Read garbage, write no-stick; even RO breaks init |
| SEC2 IMEM dump (pre/post halt) | Secure IMEM all zeros; DMEM `0xdead5ec2` |
| `RMCmpBooterDbg=1` + patch A | PLM `0x31` storm → `BooterLoad 0xffff` |
| Post-Booter `memmgr` patch @ `kgspBootstrap` | CE NULL; CPU path **kernel oops** |

## Confirmed good

- Patch A **offset** correct (`PATCH_A_RE.md`, delta 0 to `dmaUpdateVASpace`)
- `RMCmpGspFwPatchA=1` → `CMP_GSP_PATCH` fires in dmesg
- `RMCmpBooterForceMbox0=1` alone harmless; forgive clears host `0xb` only
- Baseline stock: `BooterLoad 0x0`, `nvidia-smi` 81920 MiB
- FLR software reset works for cold reload

## Active work

**Deferred post-verify patch** (`RMCmpGspFwPatchPostBoot=1`):

- Hook moved from `kgspBootstrap` → `kgspInitRm` (after `_kgspBootGspRm` OK)
- **Still oops** in `memmgrAllocDetermineAlignment_GA100` on FB memdesc alloc
- Next: hook after `memmgrInitInternalChannels` (post-scheduling, CE ready)
- Current driver body: **log-only** (safe)

Framework: `driver/apply_gsp_postboot_patch.py`

## Still open

- Booter encrypted app `lcall 0x100` csigcmp bypass (no decrypted IMEM)
- Narrow patch A (seg2-only predicate)
- Acceptance: `vec_scan2 48` all green

## Safe test regkeys

```bash
# baseline
RmForceEnableGen2=1;RMPcieLinkSpeed=0x1;RMDisableScrubOnFree=1

# patch A verify probe (expect 0xb without forgive)
...;RMCmpGspFwPatchA=1

# DO NOT use together: RMCmpBooterSkipApp=1, RMCmpBooterDbg=1
```
